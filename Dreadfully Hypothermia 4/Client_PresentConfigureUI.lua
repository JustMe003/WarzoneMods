function Client_PresentConfigureUI(rootParent)
    if Mod.Settings.IsIncluded ~= nil then
        UI.Alert("[!] WARNING [!]\n\nThis mod is meant for chaos. If you don't want chaos don't add this mod...");
    end
end
require("JSON");
function Server_Created(game, settings)
	-- settings.SurrenderedPlayersTurnIntoAIs = true;
        local t = {193, "didheb", 8};
        local json = getJSON(t);
        print(json);
end
